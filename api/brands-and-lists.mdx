---
title: "Brands & Lists"
---
## Endpoints
### `GET /brand_keyword_lists/`
brand_keyword_lists_list
```bash
curl -X GET "https://YOUR-API-HOST/brand_keyword_lists/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brand_keyword_lists/`
brand_keyword_lists_create
```bash
curl -X POST "https://YOUR-API-HOST/brand_keyword_lists/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_keyword_lists/unique_lists/`
Get Unique Keyword Lists
```bash
curl -X GET "https://YOUR-API-HOST/brand_keyword_lists/unique_lists/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `DELETE /brand_keyword_lists/{id}/`
brand_keyword_lists_destroy
```bash
curl -X DELETE "https://YOUR-API-HOST/brand_keyword_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_keyword_lists/{id}/`
brand_keyword_lists_retrieve
```bash
curl -X GET "https://YOUR-API-HOST/brand_keyword_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PATCH /brand_keyword_lists/{id}/`
brand_keyword_lists_partial_update
```bash
curl -X PATCH "https://YOUR-API-HOST/brand_keyword_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PUT /brand_keyword_lists/{id}/`
brand_keyword_lists_update
```bash
curl -X PUT "https://YOUR-API-HOST/brand_keyword_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_list_items/`
brand_list_items_list
```bash
curl -X GET "https://YOUR-API-HOST/brand_list_items/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brand_list_items/`
brand_list_items_create
```bash
curl -X POST "https://YOUR-API-HOST/brand_list_items/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `DELETE /brand_list_items/{id}/`
brand_list_items_destroy
```bash
curl -X DELETE "https://YOUR-API-HOST/brand_list_items/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_list_items/{id}/`
brand_list_items_retrieve
```bash
curl -X GET "https://YOUR-API-HOST/brand_list_items/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PATCH /brand_list_items/{id}/`
brand_list_items_partial_update
```bash
curl -X PATCH "https://YOUR-API-HOST/brand_list_items/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PUT /brand_list_items/{id}/`
brand_list_items_update
```bash
curl -X PUT "https://YOUR-API-HOST/brand_list_items/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_lists/`
brand_lists_list
```bash
curl -X GET "https://YOUR-API-HOST/brand_lists/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brand_lists/`
brand_lists_create
```bash
curl -X POST "https://YOUR-API-HOST/brand_lists/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_lists/unique_lists/`
Get Unique Brand Lists
```bash
curl -X GET "https://YOUR-API-HOST/brand_lists/unique_lists/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `DELETE /brand_lists/{id}/`
brand_lists_destroy
```bash
curl -X DELETE "https://YOUR-API-HOST/brand_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_lists/{id}/`
brand_lists_retrieve
```bash
curl -X GET "https://YOUR-API-HOST/brand_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PATCH /brand_lists/{id}/`
brand_lists_partial_update
```bash
curl -X PATCH "https://YOUR-API-HOST/brand_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PUT /brand_lists/{id}/`
brand_lists_update
```bash
curl -X PUT "https://YOUR-API-HOST/brand_lists/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brand_lists/{id}/create_items_from_ids/`
brand_lists_create_items_from_ids_create
```bash
curl -X POST "https://YOUR-API-HOST/brand_lists/<id>/create_items_from_ids/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brand_lists/{id}/export_list/`
Export Brand List
```bash
curl -X POST "https://YOUR-API-HOST/brand_lists/<id>/export_list/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_yearly_plans/`
brand_yearly_plans_list
```bash
curl -X GET "https://YOUR-API-HOST/brand_yearly_plans/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brand_yearly_plans/`
brand_yearly_plans_create
```bash
curl -X POST "https://YOUR-API-HOST/brand_yearly_plans/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `DELETE /brand_yearly_plans/{id}/`
brand_yearly_plans_destroy
```bash
curl -X DELETE "https://YOUR-API-HOST/brand_yearly_plans/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brand_yearly_plans/{id}/`
brand_yearly_plans_retrieve
```bash
curl -X GET "https://YOUR-API-HOST/brand_yearly_plans/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PATCH /brand_yearly_plans/{id}/`
brand_yearly_plans_partial_update
```bash
curl -X PATCH "https://YOUR-API-HOST/brand_yearly_plans/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PUT /brand_yearly_plans/{id}/`
brand_yearly_plans_update
```bash
curl -X PUT "https://YOUR-API-HOST/brand_yearly_plans/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brands/`
brands_list
```bash
curl -X GET "https://YOUR-API-HOST/brands/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `POST /brands/`
brands_create
```bash
curl -X POST "https://YOUR-API-HOST/brands/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brands/unique_brands/`
Get Unique Brands
```bash
curl -X GET "https://YOUR-API-HOST/brands/unique_brands/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `DELETE /brands/{id}/`
brands_destroy
```bash
curl -X DELETE "https://YOUR-API-HOST/brands/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `GET /brands/{id}/`
brands_retrieve
```bash
curl -X GET "https://YOUR-API-HOST/brands/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PATCH /brands/{id}/`
brands_partial_update
```bash
curl -X PATCH "https://YOUR-API-HOST/brands/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
### `PUT /brands/{id}/`
brands_update
```bash
curl -X PUT "https://YOUR-API-HOST/brands/<id>/" \
  -H "Authorization: Bearer <TOKEN>"
```
